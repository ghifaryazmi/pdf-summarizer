services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    expose:
      - "5000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
